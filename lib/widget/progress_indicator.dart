import 'package:flutter/material.dart';
class progress_indicator extends StatelessWidget {
  const progress_indicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: CircularProgressIndicator());
  }
}
